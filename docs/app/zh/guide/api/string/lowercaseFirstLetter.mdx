---
title: lowercaseFirstLetter
描述: 将给定 `string` 的首字母小写。
---

# `lowercaseFirstLetter`

将给定 `string` 的首字母小写。

## 使用场景

- **文本格式化**：在将标题或名称转换为特定大小写风格时使用。
- **数据规范化**：在数据处理中，标准化字符串的大小写。

## 示例

```ts
import { lowercaseFirstLetter } from 'funtool';

lowercaseFirstLetter('Hello'); // ✅ 'hello'
```

## 函数签名

```ts
function lowercaseFirstLetter(str: string): string
```

## 参数

- `str` ( `string` )：要将首字母小写的输入字符串。

## 返回值

- ( `string` )：首字母小写后的新字符串。