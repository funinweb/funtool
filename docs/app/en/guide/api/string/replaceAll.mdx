---
title: replaceAll
description: Documentation for `replaceAll` string utility.
---

# `replaceAll`

Documentation for `replaceAll` string utility.

## Example

```ts
import { replaceAll } from 'funtool/string'

replaceAll('example input')
// => ...
```

## Parameters

- Depends on the function.

## Returns

- Depends on the function.
